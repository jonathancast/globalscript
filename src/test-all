#! /usr/local/plan9/bin/rc

cd $GLOBALSCRIPT/src/libtest &&
    mk integration-test && mk install &&
    cd ../libglobalscript/ &&
    mk integration-test && mk install &&
    cd ../libibio/ &&
    mk integration-test && mk install &&
    cd ../cmd/ibio/ &&
    mk integration-test && mk install &&
    cd ../gsac/ &&
    mk integration-test
